// class_list.js
// Lightweight client-side helpers for the class list page.

document.addEventListener('DOMContentLoaded', function () {
  // Removed client-side confirmation so server will handle validation and
  // display appropriate error messages when cancel is attempted.
});
